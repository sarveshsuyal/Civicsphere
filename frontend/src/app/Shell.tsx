import {useState,useEffect} from 'react';
import {NavLink,Outlet,Link,useLocation,useNavigate} from 'react-router-dom';
import {useTranslation} from 'react-i18next';
import {LayoutDashboard,Map,TriangleAlert,ScanLine,Database,ClipboardList,ShieldCheck,ChartNoAxesCombined,FileText,Bell,Settings,HelpCircle,Search,ChevronDown,Menu,ArrowUpRight,LogOut,Clock3} from 'lucide-react';
import {Logo,Language,Modal} from '../components/ui';
import {useAuth} from '../features/auth/Auth';
import {supabase} from '../services/client';
import {demoIssues} from '../services/demo';
import {permitted,adminRoles} from '../types/domain';
const groups:{label:string;items:[string,typeof LayoutDashboard][]}[]=[{label:'workspace',items:[['dashboard',LayoutDashboard],['map',Map],['issues',TriangleAlert],['reviews',ScanLine],['datasets',Database],['jobs',Clock3],['tasks',ClipboardList],['audit',ShieldCheck]]},{label:'intelligence',items:[['risk',ShieldCheck],['analytics',ChartNoAxesCombined],['reports',FileText],['alerts',Bell]]}];
export default function Shell(){const {t}=useTranslation();const location=useLocation();const demo=location.pathname.startsWith('/demo');const base=demo?'/demo':'';const {profile}=useAuth();const [command,setCommand]=useState(false),[query,setQuery]=useState(''),[menu,setMenu]=useState(false);const navigate=useNavigate();useEffect(()=>{const handler=(e:KeyboardEvent)=>{if((e.ctrlKey||e.metaKey)&&e.key==='k'){e.preventDefault();setCommand(v=>!v);}};window.addEventListener('keydown',handler);return()=>window.removeEventListener('keydown',handler);},[]);useEffect(()=>setMenu(false),[location.pathname]);return <div className="app-shell"><aside className={`sidebar ${menu?'mobile-open':''}`}><Logo/><button className="city-switch" onClick={()=>navigate(base+'/map')}><span className="city-avatar">A</span><span><strong>{t('city')}</strong><small>{t('citySub')}</small></span><ChevronDown size={15}/></button>{groups.map(g=><div className="nav-group" key={g.label}><div className="nav-label">{t(g.label)}</div>{g.items.map(([name,Icon])=><NavLink to={`${base}/${name}`} key={name}><Icon size={18}/><span>{t(name)}</span>{demo&&name==='reviews'&&<b>3</b>}{demo&&name==='alerts'&&<i className="dot red"/>}</NavLink>)}</div>)}<div className="nav-group"><div className="nav-label">{t('management')}</div>{(demo||permitted(profile,adminRoles))&&<NavLink to={`${base}/admin`}><Settings size={18}/>{t('admin')}</NavLink>}<NavLink to={`${base}/help`}><HelpCircle size={18}/>{t('help')}</NavLink></div><div className="sidebar-bottom"><div className="workspace-status"><ShieldCheck size={17}/><span>{demo?t('readOnly'):t('live')}</span></div><Link to={demo?'/login':'/settings'} className="user-profile"><span className="avatar">{demo?'CV':profile?.display_name?.slice(0,2).toUpperCase()||'CS'}</span><span><strong>{demo?'City explorer':profile?.display_name}</strong><small>{demo?'Demo access':profile?.role.replaceAll('_',' ')}</small></span><ArrowUpRight size={16}/></Link></div></aside>{menu&&<button className="mobile-overlay" aria-label={t('close')} onClick={()=>setMenu(false)}/>}<div className="main-shell"><header className="app-header"><button className="mobile-menu icon-button" aria-label={t('navigation')} onClick={()=>setMenu(v=>!v)}><Menu/></button><div className="breadcrumb">{t('city')}<span>/</span><strong>{t(location.pathname.split('/').filter(Boolean)[demo?1:0]||'dashboard')}</strong></div><div className="header-actions"><button className="search-trigger" onClick={()=>setCommand(true)}><Search size={16}/><span>{t('search')}</span><kbd>⌘ K</kbd></button><Language/><Link to={`${base}/alerts`} className="icon-button" aria-label={t('alerts')}><Bell size={18}/></Link>{!demo&&<button className="icon-button" aria-label={t('logout')} onClick={()=>void supabase?.auth.signOut({scope:'global'}).then(()=>navigate('/login'))}><LogOut size={18}/></button>}</div></header>{demo&&<div className="demo-banner"><span><i className="dot blue"/>{t('demoLabel')}</span><Link to="/login">{t('signIn')}<ArrowUpRight size={13}/></Link></div>}<main className="workspace"><Outlet/></main><div className="workspace-footer"><span>CivicSphere · {t('footer')}</span><span>Human-led. Evidence-backed.</span></div></div><Modal open={command} onClose={()=>setCommand(false)} title={t('commandTitle')}><input autoFocus placeholder={t('search')} value={query} onChange={e=>setQuery(e.target.value)}/><div className="command-results">{groups.flatMap(g=>g.items).filter(([name])=>t(name).toLowerCase().includes(query.toLowerCase())).map(([name,Icon])=><button key={name} onClick={()=>{navigate(`${base}/${name}`);setCommand(false);}}><Icon size={17}/>{t(name)}<ArrowUpRight size={15}/></button>)}{demo&&query&&demoIssues.filter(i=>`${i.title} ${i.issue_id} ${i.ward}`.toLowerCase().includes(query.toLowerCase())).map(i=><button key={i.id} onClick={()=>{navigate(`/demo/issues/${i.id}`);setCommand(false);}}>{i.issue_id} · {i.title}</button>)}</div><p className="muted">{t('commandHelp')}</p></Modal></div>;}
